let apiBlogApi = new TempApi.BlogApi();import TempApi from '../src/index';window.onload = () => {let blogId = window.location.pathname.replace('/viewpost/','');apiBlogApi.getblog( blogId, (error, data, response) => { if (error) {console.error(error);} else { console.log('API called successfully. Returned data: ' + data); document.querySelector('[annotationname = date]').textContent = response.body.query.date ;document.querySelector('[annotationname = title]').textContent = response.body.query.title ;document.querySelector('[annotationname = image]').src = response.body.query.image ;}});};document.getElementById('iv2al').onclick = (event) => {
    event.preventDefault();
    let blogId = window.location.pathname.replace('/viewpost/','');apiBlogApi.deleteblog( blogId, (error, data, response) => { if (error) {console.error(error);} else { console.log('API called successfully.');}});};